# DataFlow 사용자 가이드 저장소

NHN Cloud DataFlow 서비스의 공개 사용자 가이드 저장소입니다. `ko/`, `en/`, `ja/`, 언어별 디렉터리에 같은 구성의 문서가 있습니다.

## 가이드 작성·수정 규칙

### 사실 서술은 반드시 실제 코드로 확인한다

노드·코덱의 속성명, 기본값, 허용 범위, 동작 방식 등 사실에 해당하는 서술은 실제 구현 코드를 확인하기 전에는 작성하거나 수정하지 않는다.

- 구현 코드 저장소의 경로가 현재 컨텍스트에 없으면, **작업을 시작하기 전에 개발자에게 코드 저장소 경로를 요청한다.** 기존 문서나 추론만으로 사실 서술을 생성하지 않는다.
- 개발자가 경로를 제공할 수 없다고 하면, 확인하지 못한 서술에는 그 사실을 명시하고 개발자 검토를 요청한다.
- 코드 경로 매핑은 각 개발자의 `CLAUDE.local.md`(커밋 금지)에 둔다. 이 파일에 있는 경로 정보나 내부 구현 내용을 공개 문서나 커밋되는 파일에 옮겨 적지 않는다.

### 기존 문서와의 일관성을 유지한다

새 노드/코덱 섹션을 추가할 때는 같은 문서의 기존 섹션 2~3개를 먼저 읽고 다음을 맞춘다.

- 섹션 구성(설명 → 속성 표 → 예시 순서 등)과 표의 컬럼 구성
- 용어(같은 개념에 같은 단어 — 예: 속성/필드/항목을 혼용하지 않기)
- 문체와 존칭 수준

### PR 전 검증

노드/코덱 가이드를 변경한 PR을 올리기 전에 **새 세션에서** `/verify-node-guide`를 실행하고 지적 사항을 반영한다. 작성에 사용한 세션에서 검증하지 않는다.
